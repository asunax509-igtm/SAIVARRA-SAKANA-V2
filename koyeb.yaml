name: SAIVARRA_SAKANA-V2
services:
  - name: SAIVARRA_SAKANA-V2
    type: web
    ports:
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin
